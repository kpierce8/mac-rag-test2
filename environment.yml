name: geo-pipeline
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
    - "pydantic-ai[ollama]"
    - docling
    - pymongo
    - qdrant-client
    - "pydantic>=2.0"
    - httpx
    - rich
    - sentence-transformers
    - tenacity
    - pdf2image
    - pillow
    - pytest
    - pytest-asyncio
    - python-dotenv
